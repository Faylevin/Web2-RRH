<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestión de Animales - Veterinaria</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        .main-content {
            flex: 1;
            padding: 2rem 0;
        }
        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }
        .card:hover {
            transform: translateY(-5px);
        }
        .table th {
            border-top: none;
            font-weight: 600;
        }
        .btn {
            border-radius: 8px;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        .btn:hover {
            transform: translateY(-2px);
        }
    </style>
</head>
<body>
    <!-- Header -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow">
        <div class="container">
            <a class="navbar-brand d-flex align-items-center" href="#">
                <i class="fas fa-paw fa-2x me-3"></i>
                <div>
                    <h4 class="mb-0">Veterinaria Amigos Peludos</h4>
                    <small class="opacity-75">Sistema de Gestión Animal</small>
                </div>
            </a>
            <div class="navbar-nav ms-auto">
                <span class="navbar-text me-3">
                    <i class="fas fa-user me-2"></i>Bienvenido, <strong><%= user.nombre %></strong>
                </span>
                <a class="nav-link text-white" href="/logout">
                    <i class="fas fa-sign-out-alt me-1"></i>Cerrar Sesión
                </a>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="main-content">
        <div class="container">
            <div class="row mb-4">
                <div class="col-12">
                    <h1 class="text-center text-primary mb-4">
                        <i class="fas fa-paw me-3"></i>Gestión de Animales
                    </h1>
                </div>
            </div>

            <!-- Formulario para agregar -->
            <div class="card mb-5">
                <div class="card-header bg-success text-white py-3">
                    <h5 class="mb-0">
                        <i class="fas fa-plus-circle me-2"></i>Agregar Nuevo Animal
                    </h5>
                </div>
                <div class="card-body p-4">
                    <form action="/add" method="post" class="row g-3">
                        <div class="col-md-4">
                            <label for="nombre" class="form-label fw-bold">
                                <i class="fas fa-signature me-2"></i>Nombre:
                            </label>
                            <input type="text" class="form-control" id="nombre" name="nombre" required 
                                   placeholder="Nombre del animal">
                        </div>
                        <div class="col-md-4">
                            <label for="especie" class="form-label fw-bold">
                                <i class="fas fa-dog me-2"></i>Especie:
                            </label>
                            <input type="text" class="form-control" id="especie" name="especie" required 
                                   placeholder="Ej: Perro, Gato, etc.">
                        </div>
                        <div class="col-md-4">
                            <label for="raza" class="form-label fw-bold">
                                <i class="fas fa-dna me-2"></i>Raza:
                            </label>
                            <input type="text" class="form-control" id="raza" name="raza" required 
                                   placeholder="Raza del animal">
                        </div>
                        <div class="col-md-4">
                            <label for="edad" class="form-label fw-bold">
                                <i class="fas fa-birthday-cake me-2"></i>Edad:
                            </label>
                            <input type="number" class="form-control" id="edad" name="edad" required 
                                   placeholder="Edad en años" min="0">
                        </div>
                        <div class="col-md-4">
                            <label for="propietario" class="form-label fw-bold">
                                <i class="fas fa-user me-2"></i>Propietario:
                            </label>
                            <input type="text" class="form-control" id="propietario" name="propietario" required 
                                   placeholder="Nombre del propietario">
                        </div>
                        <div class="col-md-4">
                            <label for="email_propietario" class="form-label fw-bold">
                                <i class="fas fa-envelope me-2"></i>Email Propietario:
                            </label>
                            <input type="email" class="form-control" id="email_propietario" name="email_propietario" required 
                                   placeholder="email@ejemplo.com">
                        </div>
                        <div class="col-12 text-center mt-4">
                            <button type="submit" class="btn btn-success btn-lg px-5">
                                <i class="fas fa-save me-2"></i>Agregar Animal
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Tabla de animales -->
            <div class="card">
                <div class="card-header bg-primary text-white py-3">
                    <h5 class="mb-0">
                        <i class="fas fa-list me-2"></i>Lista de Animales Registrados
                    </h5>
                </div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-hover table-striped mb-0">
                            <thead class="table-dark">
                                <tr>
                                    <th><i class="fas fa-hashtag me-2"></i>ID</th>
                                    <th><i class="fas fa-signature me-2"></i>Nombre</th>
                                    <th><i class="fas fa-dog me-2"></i>Especie</th>
                                    <th><i class="fas fa-dna me-2"></i>Raza</th>
                                    <th><i class="fas fa-birthday-cake me-2"></i>Edad</th>
                                    <th><i class="fas fa-user me-2"></i>Propietario</th>
                                    <th><i class="fas fa-envelope me-2"></i>Email Propietario</th>
                                    <th><i class="fas fa-cogs me-2"></i>Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% animales.forEach(animal=>{ %>
                                <tr>
                                    <td class="fw-bold"><%= animal.id %></td>
                                    <td><%= animal.nombre %></td>
                                    <td>
                                        <span class="badge bg-info text-dark">
                                            <%= animal.especie %>
                                        </span>
                                    </td>
                                    <td><%= animal.raza %></td>
                                    <td>
                                        <span class="badge bg-warning text-dark">
                                            <%= animal.edad %> años
                                        </span>
                                    </td>
                                    <td><%= animal.propietario %></td>
                                    <td>
                                        <a href="mailto:<%= animal.email_propietario %>" class="text-decoration-none">
                                            <%= animal.email_propietario %>
                                        </a>
                                    </td>
                                    <td>
                                        <div class="btn-group" role="group">
                                            <a href="/edit/<%= animal.id %>" class="btn btn-warning btn-sm">
                                                <i class="fas fa-edit me-1"></i>Editar
                                            </a>
                                            <a href="/delete/<%= animal.id %>" class="btn btn-danger btn-sm" 
                                               onclick="return confirm('¿Estás seguro de eliminar a <%= animal.nombre %>?')">
                                                <i class="fas fa-trash me-1"></i>Eliminar
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                                <% });%>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-white py-4 mt-5">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <h5 class="mb-1">
                        <i class="fas fa-heartbeat me-2"></i>Veterinaria Amigos Peludos
                    </h5>
                    <p class="mb-0 text-muted">© 2025 - Todos los derechos reservados</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <a href="#" class="text-white text-decoration-none me-3">
                        <i class="fas fa-phone me-1"></i>Contacto
                    </a>
                    <a href="#" class="text-white text-decoration-none me-3">
                        <i class="fas fa-info-circle me-1"></i>Acerca de
                    </a>
                    <a href="#" class="text-white text-decoration-none">
                        <i class="fas fa-shield-alt me-1"></i>Privacidad
                    </a>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>