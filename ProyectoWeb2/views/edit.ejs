<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Animal - Veterinaria</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 2rem 0;
        }
        .edit-container {
            background: white;
            border-radius: 15px;
            padding: 2rem;
            box-shadow: 0 15px 35px rgba(0,0,0,0.2);
            max-width: 800px;
            margin: 0 auto;
        }
        .btn {
            border-radius: 8px;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        .btn:hover {
            transform: translateY(-2px);
        }
        .form-control {
            border-radius: 8px;
            border: 2px solid #e9ecef;
            transition: all 0.3s ease;
        }
        .form-control:focus {
            border-color: #4facfe;
            box-shadow: 0 0 0 0.2rem rgba(79, 172, 254, 0.25);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="edit-container">
            <!-- Header simple -->
            <div class="text-center mb-4">
                <h2 class="text-primary">
                    <i class="fas fa-edit me-2"></i>Editar Animal
                </h2>
                <p class="text-muted">Modifica la información del animal</p>
            </div>

            <form action="/update/<%= animal.id %>" method="post" class="row g-3">
                <div class="col-md-6">
                    <label for="nombre" class="form-label fw-bold">
                        <i class="fas fa-signature me-2"></i>Nombre:
                    </label>
                    <input type="text" class="form-control" name="nombre" id="nombre" value="<%= animal.nombre %>" required>
                </div>
                <div class="col-md-6">
                    <label for="especie" class="form-label fw-bold">
                        <i class="fas fa-dog me-2"></i>Especie:
                    </label>
                    <input type="text" class="form-control" name="especie" id="especie" value="<%= animal.especie %>" required>
                </div>
                <div class="col-md-6">
                    <label for="raza" class="form-label fw-bold">
                        <i class="fas fa-dna me-2"></i>Raza:
                    </label>
                    <input type="text" class="form-control" name="raza" id="raza" value="<%= animal.raza %>" required>
                </div>
                <div class="col-md-6">
                    <label for="edad" class="form-label fw-bold">
                        <i class="fas fa-birthday-cake me-2"></i>Edad:
                    </label>
                    <input type="number" class="form-control" name="edad" id="edad" value="<%= animal.edad %>" required min="0">
                </div>
                <div class="col-md-6">
                    <label for="propietario" class="form-label fw-bold">
                        <i class="fas fa-user me-2"></i>Propietario:
                    </label>
                    <input type="text" class="form-control" name="propietario" id="propietario" value="<%= animal.propietario %>" required>
                </div>
                <div class="col-md-6">
                    <label for="email_propietario" class="form-label fw-bold">
                        <i class="fas fa-envelope me-2"></i>Email Propietario:
                    </label>
                    <input type="email" class="form-control" name="email_propietario" id="email_propietario" value="<%= animal.email_propietario %>" required>
                </div>
                <div class="col-12 text-center mt-4">
                    <button type="submit" class="btn btn-primary btn-lg px-5 me-3">
                        <i class="fas fa-save me-2"></i>Actualizar
                    </button>
                    <a href="/" class="btn btn-secondary btn-lg px-5">
                        <i class="fas fa-times me-2"></i>Cancelar
                    </a>
                </div>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>